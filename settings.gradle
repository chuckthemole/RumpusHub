/*
 * This file was generated by the Gradle 'init' task.
 *
 * The settings file is used to specify which projects to include in your build.
 *
 * Detailed information about configuring a multi-project build in Gradle can be found
 * in the user manual at https://docs.gradle.org/7.5.1/userguide/multi_project_builds.html
 */

// pluginManagement {
//     repositories {
//         gradlePluginPortal()
//         google()
//         mavenCentral()
//     }
// }

rootProject.name = 'Rumpus'

def envFile = new File(rootDir, ".env")
def env = 'DEV'

if (envFile.exists()) {
    envFile.eachLine {
        def (key, value) = it.split('=').collect { it.trim() }
        if (key == 'ENV') {
            env = value.replaceAll('"', '')
        }
    }
}

println "📘 settings.gradle ENV: $env"

if (env == 'DEV') {
    include ':common'
    project(':common').projectDir = new File(rootDir, 'common')
}

include ':rumpus'
// include ':common'
include ':admin'
